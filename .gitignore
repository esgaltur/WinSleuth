# Build output
/target
/dist

# Cargo.lock IS committed for this crate.
# It is a binary, not a library: a reproducible build is the point.

# IDEs
.idea/
.vscode/
*.iml

# OS files
Thumbs.db
.DS_Store

# Local scan history written by the tool itself
*.winsleuth.json
